# WokPost (.env.example)
# Copy to .env.local and fill in values for local development.
# Do NOT commit .env.local with real secrets.
#
# WokPost runs on Cloudflare Pages + D1 in production.
# For local dev, use: cd wokpost && wrangler pages dev

# === Auth (NextAuth v5) ===
# Generate: openssl rand -base64 32
AUTH_SECRET=generate_with_openssl_rand_base64_32
AUTH_URL=http://localhost:3000

# GitHub OAuth (at least one provider required for local login)
# Create at: https://github.com/settings/applications/new
# Callback URL: http://localhost:3000/api/auth/callback/github
AUTH_GITHUB_ID=your_github_client_id
AUTH_GITHUB_SECRET=your_github_client_secret

# Google OAuth (optional)
# Create at: https://console.cloud.google.com/apis/credentials
# Authorized redirect URI: http://localhost:3000/api/auth/callback/google
# AUTH_GOOGLE_ID=
# AUTH_GOOGLE_SECRET=

# Facebook OAuth (optional)
# AUTH_FACEBOOK_ID=
# AUTH_FACEBOOK_SECRET=

# Apple OAuth (optional)
# AUTH_APPLE_ID=
# AUTH_APPLE_SECRET=

# === WokSpec Integration ===
NEXT_PUBLIC_WOKAPI_URL=https://api.wokspec.org
# For local WokAPI dev: NEXT_PUBLIC_WOKAPI_URL=http://localhost:8787

# === Cloudflare Bindings (set in wrangler.toml for local dev) ===
# D1 database: wrangler d1 execute wokpost-db --local --file=migrations/0001_init.sql
# KV namespace FEED_CACHE: configured in wrangler.toml [[kv_namespaces]]
# These are injected at runtime via the Cloudflare Workers runtime, not process.env.

# === Observability (optional) ===
# NEXT_PUBLIC_SENTRY_DSN=

# Cloudflare Web Analytics
NEXT_PUBLIC_CF_ANALYTICS_TOKEN=

# === Eral AI ===
ERAL_API_URL=https://eral.wokspec.org/api
